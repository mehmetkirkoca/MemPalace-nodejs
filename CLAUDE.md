# MemPalace Notes

- This project uses Docker as the default execution environment.
- Prefer `docker compose` commands for running the app, tests, and related tooling.
- Do not assume host-level commands are the primary workflow unless the user explicitly asks for that.
